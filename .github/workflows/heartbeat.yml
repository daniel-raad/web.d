name: Heartbeat Reminders

on:
  schedule:
    # 6 AM GMT — morning anchor (post-training, pre-work)
    - cron: "0 6 * * *"
    # 9 PM GMT — evening review + plan tomorrow
    - cron: "0 21 * * *"
  workflow_dispatch:

jobs:
  heartbeat:
    runs-on: ubuntu-latest
    steps:
      - name: Send heartbeat
        run: |
          curl -f -X POST \
            -H "x-heartbeat-secret: ${{ secrets.HEARTBEAT_SECRET }}" \
            -H "Content-Type: application/json" \
            https://www.danielraad.co.uk/api/heartbeat
